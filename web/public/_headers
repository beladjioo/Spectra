# Hashed assets are immutable; the HTML entry point must always revalidate so
# a new deploy is picked up immediately (no more stale-cache "dead links").
/
  Cache-Control: no-cache
/index.html
  Cache-Control: no-cache
/assets/*
  Cache-Control: public, max-age=31536000, immutable
