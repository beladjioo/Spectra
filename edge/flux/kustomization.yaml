apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Edge overlay reconciled by Flux on each sensor's k3s.
# The MQTT credentials Secret (spectra-mqtt-creds) is NOT in git — create it out of
# band, or wire SOPS/sealed-secrets. See edge/README.md.
resources:
  - namespace.yaml
  - configmap.yaml
  - daemonset.yaml
